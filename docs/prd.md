# 需求文档

> **版本：** v1.2.0
> **最后更新：** 2026-07-02
> **变更摘要：** 补充资源限制、并发策略、会员配额定义、数据模型扩展、发布状态机、Feature Gate 迁移

## 1. 应用概述

### 1.1 应用名称
AI视频创作平台

### 1.2 应用描述
一款基于AI技术的视频创作工具，支持用户通过文本输入或文件上传快速生成短视频内容。平台提供文案生成、语音合成、图片生成、视频合成等核心能力，帮助用户高效完成视频创作。

## 2. 用户与使用场景

### 2.1 目标用户
- 内容创作者：需要快速产出短视频内容的自媒体从业者
- 营销人员：需要制作宣传视频的市场推广人员
- 普通用户：希望制作个人视频的非专业用户

### 2.2 核心使用场景
- 用户输入主题或上传文案，快速生成配音配图的短视频
- 用户上传音频文件，根据音频内容生成匹配的视频画面
- 用户需要批量生成多个视频片段并合并为完整视频

## 3. 页面结构与功能说明

### 3.1 页面结构

```
├── 注册/登录页面
├── 首页
│   ├── 创作模式选择
│   └── 历史记录入口
├── 创作页面
│   ├── 文案生成模式Tab
│   │   ├── 文案输入区
│   │   ├── 参数配置区
│   │   └── 生成按钮
│   ├── 音频生成模式Tab（新增）
│   │   ├── 音频上传区
│   │   ├── 参数配置区
│   │   └── 生成按钮
│   └── 分段视频模式Tab（新增）
│       ├── 分段文案输入区
│       ├── 分段音频上传区
│       ├── 参数配置区
│       └── 生成按钮
├── 视频分段管理页面（新增）
│   ├── 分段列表
│   ├── 分段编辑
│   ├── 分段删除
│   ├── 分段排序
│   └── 合并视频按钮
├── 任务详情页面
│   ├── 任务信息展示
│   ├── 文案与图片对应展示
│   ├── 单张图片重新生成
│   └── 手动合并视频按钮
├── 图片管理页面
│   ├── 图片展示
│   ├── 图片重新生成
│   └── 生成视频按钮
├── 结果展示页面
│   ├── 视频预览
│   ├── 视频下载
│   ├── 音频下载
│   └── 重新创作入口
├── 个人主页
│   ├── 用户信息展示
│   ├── 统计概览（作品数、观看数等）
│   ├── 作品画廊（网格/列表双视图）
│   └── 作品快捷操作（查看、下载）
└── 历史记录页面
    ├── 历史记录列表
    ├── 视频下载
    └── 查看详情
```

### 3.2 页面功能说明

#### 3.2.1 注册/登录页面

**注册功能**
- 用户输入手机号、验证码、密码完成注册

**登录功能**
- 用户输入手机号、验证码完成登录

#### 3.2.2 首页

**创作模式选择**
- 提供三种创作模式：
  - 文案生成模式：用户输入主题，系统生成文案、语音、图片并合成视频
  - 音频生成模式：用户上传音频文件，系统根据音频生成图片并合成视频
  - 分段视频模式：用户输入多段文案或上传多个音频，系统生成多个视频片段供用户管理和合并
- 用户选择模式后进入对应的创作页面

**历史记录入口**
- 用户点击进入历史记录页面

#### 3.2.3 创作页面

**文案生成模式Tab**

- **文案输入区**
  - 用户输入主题关键词或简短描述

- **参数配置区**
  - 用户选择语音类型（男声/女声）
  - 用户选择图片动态效果（缩放/平移/旋转/无）
  - 用户选择图片切换特效（淡入淡出/滑动/无）

- **生成按钮**
  - 用户点击生成按钮
  - 系统执行文案生成模式流程

**音频生成模式Tab（新增）**

- **音频上传区**
  - 用户上传音频文件（支持wav/m4a/mp3格式）
  - 显示已上传的音频文件名称

- **参数配置区**
  - 用户选择图片动态效果（缩放/平移/旋转/无）
  - 用户选择图片切换特效（淡入淡出/滑动/无）

- **生成按钮**
  - 用户点击生成按钮
  - 系统执行音频生成模式流程

**分段视频模式Tab（新增）**

- **分段文案输入区**
  - 用户输入多段文案，每段一行或分条输入
  - 显示已输入的分段数量

- **分段音频上传区**
  - 用户上传多个音频文件（支持wav/m4a/mp3格式）
  - 每个音频对应一个分段
  - 显示已上传的音频文件列表

- **参数配置区**
  - 用户选择语音类型（男声/女声，仅在输入文案时生效）
  - 用户选择图片动态效果（缩放/平移/旋转/无）
  - 用户选择图片切换特效（淡入淡出/滑动/无）

- **生成按钮**
  - 用户点击生成按钮
  - 系统执行分段视频模式流程

#### 3.2.4 视频分段管理页面（新增）

**分段列表**
- 显示所有生成的视频片段
- 每个片段显示：
  - 片段序号
  - 片段文案
  - 片段状态（生成中/已完成/失败）
  - 片段缩略图（已完成时显示）
  - 编辑按钮
  - 删除按钮

**分段编辑**
- 用户点击编辑按钮
- 可修改该片段的文案或重新上传音频
- 可调整该片段的参数配置
- 用户点击保存按钮
- 系统重新生成该片段的图片和视频

**分段删除**
- 用户点击删除按钮
- 系统删除该片段

**分段排序**
- 用户可拖拽调整片段顺序

**合并视频**
- 用户确认所有片段无误后，点击合并视频按钮
- 系统按顺序合并所有片段为完整视频
- 生成完成后跳转至结果展示页面

#### 3.2.5 任务详情页面

**任务信息展示**
- 显示任务创建时间
- 显示创作模式
- 显示任务状态

**文案与图片对应展示**
- 按顺序展示所有文案段落及对应的图片
- 每个文案段落显示：
  - 段落序号
  - 文案内容
  - 对应的图片预览（若生成成功）
  - 图片生成失败时，显示占位提示（如「生成失败，点击重试」）
  - 重新生成按钮

**单张图片重新生成**
- 用户点击失败图片的重新生成按钮
- 显示该图片的提示词输入框（默认填充原提示词）
- 用户可保持原提示词或修改提示词
- 用户点击确认按钮
- 系统调用图片生成重新生成该图片
- 生成完成后更新该图片

**手动合并视频**
- 所有图片生成成功后，显示「合并视频」按钮
- 用户点击按钮，系统根据图片、语音音频、图片动态效果、图片切换特效生成视频
- 生成完成后跳转至结果展示页面

#### 3.2.6 图片管理页面

**图片展示**
- 按顺序展示所有生成的图片
- 每张图片显示：
  - 图片序号
  - 图片预览
  - 重新生成按钮

**图片重新生成功能**
- 用户可点击某张图片的重新生成按钮
- 显示该图片的提示词输入框
- 用户可保持原提示词或修改提示词
- 用户点击确认按钮
- 系统调用图片生成重新生成该图片
- 生成完成后更新该图片

**视频生成**
- 用户确认所有图片无误后，点击生成视频按钮
- 系统根据图片、语音音频、图片动态效果、图片切换特效生成视频
- 生成完成后跳转至结果展示页面

#### 3.2.7 结果展示页面

**视频预览**
- 展示生成的完整视频内容
- 支持在线播放

**视频下载**
- 提供下载按钮
- 用户点击下载视频文件

**音频下载**
- 若生成了合成音频，提供MP3下载按钮
- 用户点击下载音频文件

**重新创作**
- 提供返回创作页面的入口

#### 3.2.8 历史记录页面

**历史记录列表**
- 显示用户所有历史任务记录
- 每条记录显示：
  - 任务创建时间（年-月-日 时:分:秒）
  - 创作模式
  - 任务状态：排队中、处理中、已完成、失败
  - 排队中的任务显示排队位置（如「排在 3/11 位」）和预估剩余时间
  - 分段子视频标注「分段子视频及顺序序号」（如「分段子视频 1/3」）

**视频下载**
- 生成成功的视频提供下载按钮
- 用户点击下载视频文件

**查看详情**
- 用户点击某条记录可查看详细信息
- 跳转至对应的结果展示页面或视频分段管理页面或任务详情页面

## 4. 业务规则与逻辑

### 4.1 用户注册登录规则

**注册规则**
- 手机号必须为11位数字
- 验证码必须为6位数字
- 密码长度为6-20位

**登录规则**
- 手机号验证码登录：验证码有效期为5分钟
- 同一手机号60秒内只能发送一次验证码

### 4.2 文案生成模式流程

1. 用户输入主题关键词
2. 系统调用AI文案生成能力，生成完整文案
3. 系统根据文案生成语音音频
4. 系统根据文案生成图片提示词
5. 系统调用图片生成能力，批量生成图片
6. 若所有图片生成成功，系统自动合成视频
7. 若有图片生成失败，系统停止自动合成，提示用户进入任务详情页面处理失败图片

### 4.3 音频生成模式流程（新增）

1. 用户上传音频文件（wav/m4a/mp3格式）
2. 系统调用short-speech-recognition技能识别音频内容，生成文案
3. 系统根据文案生成图片提示词
4. 系统调用图片生成能力，批量生成图片
5. 若所有图片生成成功，系统自动合成视频
6. 若有图片生成失败，系统停止自动合成，提示用户进入任务详情页面处理失败图片


### 4.3.1 音频文件上传限制（新增）

| 约束项 | 限制值 | 说明 |
|--------|-------|------|
| 文件大小上限 | 50 MB | 超过后提示「文件过大，请压缩后重试」 |
| 音频时长上限 | 15 分钟 | 超过后提示「音频过长，请截取关键片段」 |
| 支持格式 | wav / m4a / mp3 | 不支持的格式提示「仅支持 WAV、M4A、MP3 格式」 |
| 分段模式单段上限 | 50 MB / 3 分钟 | 同上，额外限制时长与分段上限对齐 |

**校验时机：**
1. 上传时前端通过 `file.size` 校验文件大小
2. 上传后后端使用 ffprobe 检测实际时长
3. 超限文件拒绝入库，返回 HTTP 400 + 错误码 `AUDIO_FILE_EXCEEDED`

### 4.4 分段视频模式流程（新增）

1. 用户输入多段文案或上传多个音频文件
2. 系统为每段文案/音频生成对应的视频片段：
   - 若为文案输入，系统生成语音音频
   - 若为音频上传，系统调用short-speech-recognition技能识别音频内容
   - 系统根据文案生成图片提示词
   - 系统调用图片生成能力生成图片
   - 系统合成该片段的视频
3. 用户在视频分段管理页面查看、编辑、排序片段
4. 用户点击合并视频按钮
5. 系统按顺序合并所有片段为完整视频

### 4.5 图片生成失败的容错与恢复机制

**失败检测与阻断**
- 批量生成图片过程中，若有一张或多张图片生成失败
- 系统停止后续的「合并视频」步骤，不执行自动合成操作

**用户提示**
- 系统向用户展示明确提示，告知哪些图片生成失败
- 提示用户进入「任务详情」页面，对失败图片进行单独重新生成
- 提示用户所有图片就绪后，可手动触发合并视频

**任务详情页容错展示**
- 保留失败图片的顺序位置（占位不消失）
- 保留该图片对应的文案/提示词文字
- 图片区域展示为空白或使用占位提示图（如「生成失败，点击重试」）

**单张重新生成**
- 用户在任务详情页面，可对失败的图片单独触发重新生成
- 用户可保持原提示词或修改提示词
- 系统调用图片生成重新生成该图片
- 生成完成后更新该图片

**手动合并视频**
- 所有图片生成成功后，任务详情页面显示「合并视频」按钮
- 用户点击按钮，系统根据图片、语音音频、图片动态效果、图片切换特效生成视频
- 生成完成后跳转至结果展示页面

### 4.6 语音合成规则

**语音类型**
- 男声：使用男性音色
- 女声：使用女性音色

**语音参数**
- 语速：正常语速
- 音量：标准音量

### 4.7 图片生成规则

**图片数量**
- 根据文案长度和音频时长自动计算图片数量
- 每张图片对应3-5秒的音频时长

**图片提示词生成**
- 系统根据文案内容生成图片提示词
- 每段文案对应一个图片提示词

**图片动态效果**
- 缩放：图片从小到大或从大到小缩放
- 平移：图片从左到右或从上到下平移
- 旋转：图片顺时针或逆时针旋转
- 无：图片静止不动

**图片切换特效**
- 淡入淡出：前一张图片淡出，后一张图片淡入
- 滑动：后一张图片从侧面滑入
- 无：直接切换


### 4.8a 数据模型扩展 — VideoAsset / ScenePrompt（新增）

为支持分段模式的混合输入（文案 + 音频），需扩展 `shared-models` 中的模型定义：

**ScenePrompt 新增字段：**

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `audio_file` | `Optional[str]` | `None` | 分段模式下用户上传的音频文件路径/URL |
| `subtitle_file` | `Optional[str]` | `None` | 自定义字幕文件路径（SRT/VTT），优先级高于自动生成 |

**VideoAsset 新增字段：**

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `max_duration` | `Optional[float]` | `None` | 视频最大时长（秒），用于前端校验 |
| `segment_mode` | `bool` | `False` | 是否为分段模式 |
| `merged_audio_url` | `Optional[str]` | `None` | 合并后的完整音频 URL（多段合并时使用） |

**变更流程：** 按 shared-models 冻结协议，需三组 ACK + 协调者批准后方可合并。

### 4.8 视频合成规则

**视频时长**
- 视频时长等于音频时长

**视频分辨率**
- 默认分辨率为1920x1080

**视频帧率**
- 默认帧率为30fps

**合成顺序**
- 按图片顺序依次合成
- 每张图片对应的音频片段同步播放
- 图片动态效果和切换特效按用户配置执行


### 4.8.1 视频时长限制（新增）

| 模式 | 最大时长 | 说明 |
|------|---------|------|
| 文案生成模式 | 10 分钟 | 音频时长即视频时长，超过 10 分钟时提示用户缩短文案 |
| 音频生成模式 | 10 分钟 | 上传音频最长 15 分钟，但合成视频上限 10 分钟 |
| 分段视频模式（单段） | 3 分钟 | 每段独立限制，超过 3 分钟提示用户拆分 |
| 分段视频模式（合并后） | 10 分钟 | 所有分段时长之和不得超过 10 分钟 |

**前端校验：**
- 音频上传后即时校验时长，超过上限时禁用「生成」按钮并提示
- 分段模式下实时计算总时长，接近上限时显示进度条预警

**后端校验：**
- Story2Video 合成前二次校验，超限返回 HTTP 400 + 错误码 `VIDEO_DURATION_EXCEEDED`

### 4.9 分段视频合并规则（新增）

**合并条件**
- 所有分段视频必须生成成功
- 分段顺序由用户在视频分段管理页面确定

**合并逻辑**
- 系统按分段顺序依次拼接视频
- 保持每个分段的原始音频、图片、动态效果和切换特效
- 分段之间无额外转场效果

**合并结果**
- 生成一个完整视频文件
- 视频时长等于所有分段视频时长之和

### 4.10 历史记录规则

**记录保存**
- 所有任务记录永久保存
- 用户可随时查看历史记录

**任务状态**
- 排队中：任务已提交，等待处理
- 处理中：任务正在生成
- 已完成：任务生成成功
- 失败：任务生成失败

**排队位置**
- 排队中的任务显示当前排队位置和总排队数量
- 显示预估剩余时间


### 4.11 并发控制策略（新增）

**核心原则：** 单用户同时只允许一个视频生成任务运行，多用户通过 orchestrator 统一调度。

| 策略 | 说明 |
|------|------|
| 串行队列 | 每个用户提交的任务进入个人 FIFO 队列，前一个任务完成/失败后自动启动下一个 |
| 全局并发上限 | 同时运行的视频合成任务总数由 orchestrator 控制（默认 1，4G ECS 约束） |
| 用户级互斥 | 同一用户的第二个生成请求返回 HTTP 409 + 错误码 `TASK_CONFLICT` |
| 排队状态 | 历史记录页显示排队位置（如「排在第 2 位，预计等待 5 分钟」） |
| 优先级 | 付费用户 > 免费用户（同一用户内按提交时间 FIFO） |

**orchestrator 集成：**
- Story2Video 提交任务到 `POST /api/jobs/create`（通过 orchestrator）
- orchestrator 维护全局任务队列，通过 `GET /api/jobs/status/:id` 轮询状态
- 前端使用 SSE 或 3 秒间隔轮询获取任务进度

## 5. 异常与边界情况

| 场景 | 处理方式 |
|------|----------|
| 用户输入主题为空 | 提示用户输入主题 |
| 用户上传的音频文件格式不支持 | 提示用户上传支持的格式（wav/m4a/mp3） |
| 用户上传的音频文件为空 | 提示用户上传音频文件 |
| 音频识别失败 | 提示用户重新上传音频或手动输入文案 |
| 文案生成失败 | 提示用户重新生成或手动输入文案 |
| 语音合成失败 | 提示用户重新生成 |
| 图片生成失败 | 停止自动合成，提示用户进入任务详情页面重新生成失败图片 |
| 视频合成失败 | 提示用户重新生成 |
| 分段视频模式下用户未输入文案且未上传音频 | 提示用户至少输入一段文案或上传一个音频 |
| 用户删除所有分段片段 | 提示用户至少保留一个片段 |
| 分段视频合并时有片段生成失败 | 提示用户先完成所有片段的生成 |
| 用户未选择语音类型 | 默认使用女声 |
| 用户未选择图片动态效果 | 默认使用无动态效果 |
| 用户未选择图片切换特效 | 默认使用淡入淡出 |
| 网络异常导致任务中断 | 任务状态标记为失败，用户可重新生成 |
| 用户在排队中取消任务 | 任务从队列中移除，状态标记为已取消 |
| 音频文件超过 50MB | 提示「文件过大，请压缩后重试」，拒绝上传 |
| 音频时长超过 15 分钟 | 提示「音频过长，请截取关键片段」 |
| 视频时长超过 10 分钟 | 提示用户缩短文案或拆分片段 |
| 分段模式单段超过 3 分钟 | 提示用户拆分该段 |
| 同一用户重复提交生成任务 | 返回 409，提示「有任务正在执行，请等待完成」 |
| 并发超出全局上限 | 任务进入排队，显示排队位置 |
| 会员配额用尽 | 生成按钮置灰，提示「今日生成次数已用完」 |
| 发布平台 API 不可用 | 任务标记对应阶段失败，支持重试 |
| 分段合并后总时长超限 | 提示用户删除片段或缩短单段时长 |

## 6. 验收标准

1. 用户注册并登录成功
2. 用户选择音频生成模式，上传音频文件「example.mp3」
3. 系统识别音频内容，生成图片并自动合成视频（若图片全部生成成功）
4. 用户在结果展示页面预览视频
5. 用户下载视频文件

## 7. 质量与测试

### 7.1 单元测试覆盖率要求

核心模块必须有单元测试覆盖，测试框架使用 Vitest + jsdom：

| 模块 | 测试文件 | 最低用例数 | 说明 |
|------|----------|-----------|------|
| Canvas 视频合成 | `src/lib/slideshow.test.ts` | >=15 | 覆盖 renderFrame、renderTransition、createSlideshowVideo、字幕样式 |
| 历史文章提示词 | `src/lib/history-prompt.test.ts` | >=20 | 覆盖所有导出类：SentimentAnalyzer、EraDetector、DynastyDetector、HybridDynastyDetector、VisualStyleSelector、DiversePromptGeneratorV10、HistoryArticleProcessorV9 |
| 音频混音 | `src/lib/audio-mixer.test.ts` | >=8 | 覆盖 mixAudio、uploadMixedAudio（含 fetch/AudioContext mock）|

### 7.2 CI 流水线

- 使用 GitHub Actions，每次 push/PR 到 main 分支自动运行
- 包管理器：pnpm（`pnpm install --frozen-lockfile` + `pnpm test`）
- 所有测试必须通过，失败则 CI 标记为失败

## 7.10 视频水印自定义功能

### 7.10.1 功能描述
用户可在视频输出画面上叠加自定义文字水印，提升品牌曝光和内容标识。

### 7.10.2 水印配置项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 启用水印 | 开关，控制水印是否显示 | 关闭 |
| 水印文字 | 用户自定义水印文字内容 | 空 |
| 显示位置 | 水印在画面中的位置：左上/右上/左下/右下/居中 | 右下 |
| 字号 | 水印文字大小（12px-72px） | 24px |
| 透明度 | 水印透明度（10%-100%） | 60% |
| 文字颜色 | 水印文字颜色（颜色选择器） | #ffffff |

### 7.10.3 存储方式
- 水印配置存储在 localStorage 中
- 配置项变更后立即持久化
- 不支持跨设备同步

### 7.10.4 渲染方式
- 水印在视频合成时实时绘制到画布上
- 叠加在图片和字幕之上
- 支持 Canvas 2D 渲染

### 7.10.5 界面入口
- 侧边栏「水印」按钮
- 打开水印设置对话框进行配置


## 7.11 视频分享功能

### 7.11.1 功能描述
用户可将生成的视频结果页分享给他人，支持多种分享方式。

### 7.11.2 分享方式

| 方式 | 说明 |
|------|------|
| Web Share API | 调用系统原生分享面板（移动端/桌面端优先） |
| 复制链接 | 复制结果页链接到剪贴板（降级为 execCommand） |
| 在新窗口打开 | 直接在新标签页打开视频文件 |

### 7.11.3 分享链接
- 链接指向结果展示页面（`/result/:id`），而非视频文件直链
- 支持 `shareUrl` prop 区分「分享链接」和「视频文件链接」
- 分享文案自动包含视频描述（prompt）

### 7.11.4 界面入口
- 结果展示页面底部操作栏，「下载」与「重新创作」之间
- 下拉菜单形态，支持三种分享选项
- `variant` 和 `size` 可配置以适配不同布局

### 7.11.5 降级策略
- Web Share API 不可用时 → 回退到复制链接
- 剪贴板 API（`navigator.clipboard`）不可用时 → 使用 `document.execCommand('copy')`
- 用户取消分享（AbortError）→ 静默忽略，无错误提示

## 7.12 批量导出功能

### 7.12.1 功能描述
用户可将多个视频片段打包为 ZIP 文件一次性下载，方便批量保存和分发。

### 7.12.3 界面入口
- 多片段结果页底部「批量导出 (N)」按钮
- 按钮显示当前可导出的文件数量

### 7.12.4 技术实现
- 使用纯 JS ZIP 实现（无外部依赖）
- 并行下载所有视频文件
- 下载失败的文件自动跳过，不影响其他文件导出

## 7.13 视频编辑功能（剪辑）

### 7.13.1 功能描述
用户在结果展示页面可对生成的视频进行裁剪操作，提取需要的片段。支持选择开始和结束时间，预览裁剪范围，生成裁剪后的视频并下载。

### 7.13.2 组件设计

**VideoClipEditor**
- Dialog 形式弹出，包含视频预览、时间轴选择器
- 支持双滑块选择裁剪范围（开始/结束时间）
- 实时预览裁剪区间
- 裁剪过程中显示进度条
- 裁剪完成后展示结果视频，支持预览和下载
- 支持取消操作，重置所有状态

**useVideoClip Hook**
- 管理裁剪状态（startTime/endTime/duration/isClipping/progress/error/clipResult）
- clipVideo(videoUrl)：执行裁剪，返回 Blob URL
- 使用 Canvas captureStream + MediaRecorder 实现浏览器端裁剪
- 支持中止操作（AbortController）
- 校验逻辑：开始时间 < 结束时间，结束时间 <= 视频时长

### 7.13.3 界面入口
- 单分视频结果页底部操作栏新增「剪辑」按钮
- 位于「下载视频」与「分享」之间
- 仅单视频模式显示，多片段模式不显示

### 7.13.4 输出格式
- 根据浏览器支持的编码自动选择（优先 MP4/H.264，回退 WebM/VP9）
- 视频质量 6Mbps 码率
- 保持原始分辨率

### 7.13.5 异常处理
- 开始时间 >= 结束时间：提示用户调整范围
- 结束时间超过视频时长：提示用户
- 网络加载失败：显示错误信息
- 裁剪取消：静默重置

## 7.14 用户个人主页与作品展示

### 7.14.1 功能描述
用户个人主页，集中展示用户信息和所有已生成的视频作品。

### 7.14.2 页面结构

| 区域 | 内容 |
|------|------|
| 个人资料头 | 头像、用户名/手机号、作品总数 |
| 统计概览 | 已完成作品数、全部作品数、总观看数、最近作品日期 |
| 作品列表 | 网格/列表双视图切换，展示作品的缩略图、标题、状态、操作按钮 |

### 7.14.3 作品卡片功能
- 鼠标悬停自动播放视频预览
- 点击查看进入结果详情页
- 下载按钮下载视频文件
- 状态标签（已完成/处理中/失败/排队中）

### 7.14.4 导航入口
- 侧边栏导航「我的」入口
- 路由路径 `/profile`

### 7.14.5 数据来源
- 从 `video_tasks` 表查询所有任务
- 过滤有视频 URL 的任务作为作品展示
- 使用 `merged_video_url` 或 `video_url` 作为播放源

## 7.16 视频模板

#### 7.16.1 功能描述
用户可选择预设视频模板快速开始创作。模板定义视频的转场效果、图片效果、BGM、字幕样式、时长等参数。选择模板后自动填入创建页面，用户可再微调。

#### 7.16.2 模板数据结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 唯一标识 |
| `name` | string | 模板名称 |
| `description` | string | 模板描述 |
| `thumbnail` | string? | 缩略图 URL |
| `category` | `popular|business|education|vlog|creative|custom` | 模板分类 |
| `imageEffect` | string | 图片动效 (zoom-in/zoom-out/pan-left/pan-right/none) |
| `transitionEffect` | string | 转场效果 (fade/slide-left/slide-right/slide-up/none) |
| `bgm` | `{url, name, volume}`? | BGM 预设 |
| `subtitleStyle` | `{enabled, font, size, style}`? | 字幕样式预设 |
| `perImageDuration` | number | 每张图片时长（秒） |
| `size` | string? | 视频尺寸 |
| `seconds` | number? | 视频总时长 |

#### 7.16.3 内置模板列表

| 模板 | 分类 | 动效 | 转场 | 尺寸 | 每张时长 |
|------|------|------|------|------|---------|
| 快速成片 | 热门 | zoom-in | fade | 1920x1080 | 4s |
| 幻灯片演示 | 商务 | none | slide-left | 1920x1080 | 5s |
| 动感快剪 | 创意 | zoom-out | slide-right | 1920x1080 | 3s |
| Vlog 日常 | Vlog | pan-left | fade | 1080x1920 | 4s |
| 知识讲解 | 教育 | none | fade | 1920x1080 | 6s |
| 营销推广 | 商务 | zoom-in | slide-up | 1920x1080 | 3s |
| 电影质感 | 创意 | pan-right | fade | 1920x1080 | 5s |

#### 7.16.4 组件设计

- `VideoTemplatePicker`: Dialog 中展示模板卡片网格，支持分类筛选
- `TemplateSelectButton`: 触发按钮，切换「选模板」/「切换模板」状态
- 模板选择后触发 `onSelect(template)` 回调，外部填入创建参数

#### 7.16.5 自定义模板

- 用户可通过 localStorage 保存自定义模板（ID 前缀 `custom-`）
- 内置模板不可修改，自定义模板可删除
- 后期可扩展为后端存储

## 7.17 视频特效库

#### 7.17.1 功能描述
独立的视频特效选择器，允许用户为视频单独选择图片动态效果和转场效果。与模板系统解耦，可在不切换模板的情况下调整特效参数。

#### 7.17.2 图片动效列表

| ID | 名称 | 说明 | 适用场景 |
|----|------|------|---------|
| zoom-in | 放大 | 画面从原始尺寸缓慢放大，产生推进感 | 风景、建筑、产品展示 |
| zoom-out | 缩小 | 画面从放大状态缓慢缩小到原始尺寸 | 人像、群体照 |
| pan-left | 左移 | 画面从右向左平移 | 全景、长图 |
| pan-right | 右移 | 画面从左向右平移 | 全景、长图 |
| pan-up | 上移 | 画面从下向上平移 | 建筑、人像全身 |
| pan-down | 下移 | 画面从上向下平移 | 建筑、风景 |
| zoom-pan | 放大平移 | 放大同时平移，电影级运镜 | 高端展示、品牌宣传 |
| rotate | 旋转 | 画面缓慢旋转 | 创意短片、娱乐内容 |
| blur-in | 模糊渐入 | 画面从模糊逐渐变清晰 | 回忆、梦境、开场 |
| none | 无效果 | 画面保持静止 | 所有场景 |

#### 7.17.3 转场效果列表

| ID | 名称 | 说明 |
|----|------|------|
| fade | 渐隐 | 前画面淡出同时后画面淡入 |
| slide-left | 左滑 | 后画面从右侧滑入，推到左侧 |
| slide-right | 右滑 | 后画面从左侧滑入，推到右侧 |
| slide-up | 上滑 | 后画面从下方滑入，推到上方 |
| slide-down | 下滑 | 后画面从上方滑入，推到下方 |
| none | 直接切换 | 无过渡动画 |

#### 7.17.4 组件设计

- `EffectPicker`: Dialog 中分两段展示图片动效和转场效果，支持推荐搭配提示
- `EffectSettingsButton`: 触发按钮，显示当前特效组合
- 每项效果卡片显示：名称、ID、描述、适用标签、提示标签

#### 7.17.5 推荐搭配逻辑
- 动态图片效果（如 zoom-in）推荐搭配 fade 或 none 转场
- 静态图片效果（none）可搭配所有转场效果
- 推荐搭配以标签形式在转场卡片中标注「推荐搭配」

### 7.15 B站视频发布功能

#### 7.15.1 功能描述
用户可在结果页一键将生成的视频发布到 B站（bilibili）。发布流程通过 orchestrator 的后台任务异步执行：先下载视频 → 上传到 B站 → 创建投稿。

#### 7.15.2 触发条件
- 视频已生成成功（有 video_url）
- orchestrator 已配置 B站 SESSDATA（通过 ProviderRouter 管理）
- 用户已配置 orchestrator 连接（API Key 或同域部署）

#### 7.15.3 界面入口
- 单分视频结果页底部操作栏，位于「分享」按钮之前
- 多片段模式：仅在有至少一个子视频完成时显示
- 按钮文案三态：「发布到B站」/「发布中...（禁用+旋转动画）」/「已发布（绿色勾）」

#### 7.15.4 交互流程
1. 用户点击「发布到B站」按钮
2. 前端调用 `publishVideo()` 函数 → POST `/api/jobs/publish-video`
3. orchestrator 创建 video_publish 任务（status: pending）
4. BackgroundTask 异步执行：下载视频 → B站 pre-upload → 分片上传 → create archive
5. 前端显示发布中状态（loading），防止重复提交
6. 发布成功/失败通过 toast 通知用户

#### 7.15.5 错误处理
- 网络错误：toast 提示错误信息
- orchestrator 未配置 B站 SESSDATA：返回 400 错误，前端展示失败原因
- 视频不存在/URL 过期：发布任务失败，jobs 表记录 error

#### 7.15.6 配置说明
- 环境变量 `VITE_ORCHESTRATOR_URL` 或 localStorage `orchestrator_url` 指定 orchestrator 地址
- localStorage `orchestrator_api_key` 保存 API Key（对应 orchestrator 的 `PO_API_KEY`）
- 开发环境默认使用 `/api`（生产同域）或 `http://localhost:8000`（开发跨域）

#### 7.15.7 依赖
- backend: orchestrator `POST /api/jobs/publish-video`（feature gate: video_publish）
- backend: orchestrator `services/bilibili_publisher.py` — B站上传完整流程
- backend: auth via JWT Bearer token 或 X-API-Key header
## 7.18 付费会员系统

### 7.18.1 功能描述
会员分级系统和功能权限控制。免费用户可使用基础功能，付费用户解锁高级创作能力。系统通过 orchestrator 后端管理订阅生命周期、用量配额和 feature gate 权限。

### 7.18.2 计划层级

| 计划 | 价格 | 配额 | 特色功能 |
|------|------|------|---------|
| 免费版 | ¥0 | 每日 3 次视频生成 | 基础分句、固定模板 |
| 基础版 | ¥9.99 | 每日 10 次视频生成 | 声音克隆、AI 优化 |
| 专业版 | ¥29.99 | 每日 50 次视频生成 | 批量分句、无水印 |
| 企业版 | ¥99.99 | 每日 200 次视频生成 | 全部高级功能、优先支持 |


### 7.18.2a 会员配额定义（新增）

**「次」的精确定义：**

- **1 次 = 1 次视频生成**（从「点击生成按钮」到「视频合成完成」为一次）
- 下载视频**不消耗**配额
- 发布到第三方平台**不消耗**配额
- 重新生成单张图片**不消耗**配额（属于同一生成任务内的重试）
- 合并分段视频**不消耗**配额（属于同一生成任务的后续步骤）
- 分段模式下，每个子段的生成**各计 1 次**（例如 3 段 = 3 次）

**配额周期：** 自然日（UTC+8 00:00:00 重置）

**配额不足处理：**
- 达到上限时「生成」按钮置灰，提示「今日生成次数已用完，明天再来」
- 不支持购买额外次数或跨日结余

### 7.18.3 Feature Gate 映射

| 功能 | 所需最低计划 | 说明 |
|------|------------|------|
| `voice_clone` | 基础版 | 声音克隆 / 高级语音 |
| `video_fixed_template` | 基础版 | 视频固定模板 |
| `batch_split` | 专业版 | 批量分句 |

### 7.18.4 前端组件

| 组件 | 用途 |
|------|------|
| `useOrchestratorMembership` Hook | 获取会员信息与用量配额，自动管理 JWT 生命周期 |
| `useFeatureGate` Hook | 功能级权限检测，配合 `RequireFeature` 组件做 UI 锁定 |
| `RequireFeature` 组件 | 包装高阶组件，不满足权限时显示 `LockedFeature` 占位 |
| `LockedFeature` 组件 | 锁定时默认占位 UI，附升级引导 |
| `MembershipCard` | 个人主页会员卡片，展示计划名称与权益 |
| `MembershipUpgradeDialog` | 套餐对比 → 订单创建 → 支付确认 → 升级成功全流程 |
| `QuotaWidget` | 用量进度条（如 3/10），不足时显示预警 |
| `RouteGuard` | 路由级别的会员权限守卫 |
| `ApiSettingsDialog` 会员 Tab | 按会员等级限制 API Key 配置项可见性 |

### 7.18.5 后端集成

- **orchestrator `POST /api/payment/create-checkout`**: 创建订单
- **orchestrator `POST /api/payment/confirm-mock`**: 确认支付（开发阶段 mock）
- **orchestrator `POST /api/auth/subscription`**: 获取订阅信息
- **orchestrator `POST /api/auth/login`**: 自动注册/登录 shadow 账户
- **orchestrator Feature Gates**: `premium_content` 等 gate 控制后端功能暴露

### 7.18.6 健壮性设计

- Token 自动刷新：access_token 过期后自动用 refresh_token 续期
- Stale-while-revalidate：从 localStorage 展示缓存数据，后台异步刷新
- 401 自动重试：token 失效时清除并重新登录
- 降级：orchestrator 不可用时会员信息返回 null，功能按免费权限处理
- 配额不足预警：接近上限时 CreatePage 显示 amber 提示
## 8. 本期不实现功能


以下功能已规划但未在当前版本实现，留待后续迭代：

### 8.1 多人协作功能
- 共享项目、邀请协作者、权限管理
- 实时协作编辑
- 版本历史与回滚

### 8.2 发布到第三方平台
- **状态：Phase 1 已实现**（orchestrator 后端发布管道）
- 一键发布到抖音 / B站（集成 orchestrator `/api/jobs/publish-video`）
- Story2Video 前端 GalleryPage 新增「发布到...」按钮 + PublishDialog 弹窗
- 发布进度轮询：提交 → 下载 → 发布 → 完成
- 依赖 orchestrator 的 bilibili_publisher / douyin_publisher 服务 + cookie 凭据
- **Phase 2 已实现**：定时发布功能（orchestrator scheduled_at 字段 + PublishDialog 定时开关）
- **Phase 2 已实现**：视频号 ECS 发布（tencent_video_publisher 完整 API 流程）
- **框架已扩展**：小红书平台按钮已加入 PublishDialog（已支持）
- **已实现**：小红书 ECS 发布（xiaohongshu_publisher 完整 9 步 API 流程：COS 分片上传 + Node.js 签名 + ffprobe 检测）

### 8.3 付费会员功能
- **状态：已实现**（Phase 1 + Phase 2）
- 通过 `useOrchestratorMembership` Hook 接入 orchestrator 订阅系统
- 会员权益管理：Feature Gate 控制（voice_clone/batch_split/video_fixed_template）
- 用量配额管理：QuotaWidget 展示 + CreatePage 预警提示
- 详细设计见 [`docs/membership-design.md`](membership-design.md)



## 7.20 发布状态机（新增）

### 7.20.1 问题

原文 PRD 中发布进度仅定义了「提交 → 下载 → 发布 → 完成」4 个粗粒度状态，无法精确反映发布流程中的中间环节。

### 7.20.2 PublishStage 枚举（shared-models）

需在 `shared-models/pipeline.py` 中新增 `PublishStage` 枚举：

```python
class PublishStage(str, Enum):
    """发布任务生命周期阶段 — 10 级精细状态。"""

    PENDING = "pending"               # 已提交，等待处理
    DOWNLOADING = "downloading"       # 正在从 Story2Video 下载视频
    DOWNLOAD_FAILED = "download_failed"  # 下载失败
    UPLOADING = "uploading"           # 正在上传到目标平台
    UPLOAD_FAILED = "upload_failed"   # 上传失败
    SUBMITTING = "submitting"         # 已上传，正在提交投稿/审核
    SUBMIT_FAILED = "submit_failed"   # 投稿失败
    REVIEWING = "reviewing"           # 平台审核中（B站/小红书等）
    PUBLISHED = "published"           # 发布成功
    FAILED = "failed"                 # 最终失败（不可恢复）
```

### 7.20.3 状态转换图

```
PENDING → DOWNLOADING → UPLOADING → SUBMITTING → REVIEWING → PUBLISHED
    ↓          ↓             ↓            ↓
  FAILED  DOWNLOAD_FAILED UPLOAD_FAILED SUBMIT_FAILED → FAILED
```

### 7.20.4 前端展示映射

| PublishStage | 前端文案 | 图标 |
|-------------|---------|------|
| `pending` | 排队中 | ⏳ |
| `downloading` | 正在下载视频... | 📥 |
| `uploading` | 正在上传到平台... | 📤 |
| `submitting` | 正在提交投稿... | 📝 |
| `reviewing` | 平台审核中 | 🔍 |
| `published` | 已发布 | ✅ |
| `*_failed` | 发布失败（点击重试） | ❌ |

### 7.20.5 轮询策略
- 前端每 3 秒调用 `GET /api/jobs/publish-status/:jobId`
- `PUBLISHED` 或 `FAILED` 时停止轮询
- `reviewing` 状态可降低轮询频率至 10 秒（平台审核通常需要分钟级时间）

## 7.19 Ian 小黑分镜 Prompt 模板

### 7.19.1 功能描述
为视频生成的图片提示词阶段引入 Ian 小黑插画的分镜方法论，将抽象概念转换为结构化分镜描述，提升视觉叙事的一致性。

### 7.19.2 模块设计

**文件**: `src/lib/storyboard-prompt.ts` (客户端引擎), `src/services/storyboard-service.ts` (HTTP 客户端)

客户端引擎 (`storyboard-prompt.ts`)：
**文件**: `src/lib/storyboard-prompt.ts`
核心能力：
- **8 种构图模式**: 流程展示、系统局部、前后对比、角色状态、概念隐喻、方法分层、地图路径、迷你漫画
- **14 个动态动作 + 23 个视觉物体**: 从概念→构图匹配→动作/对象选择→视觉场景组装
- **三步隐喻引擎**: `threeStepMetaphor(concept)` 将抽象概念分解为构图+动作+视觉描述
- **creativeLevel 控制**: 1-10 调节色彩、约束、关键词数量，适应不同内容风格
- **多候选生成**: `generateCandidates(concept, count=3)` 批量生成去重分镜方案

服务客户端 (`storyboard-service.ts`)：
- **服务端模式**: 调用 prompt-engine `/v1/storyboard/compose` API（通过 orchestrator 路由）
- **客户端降级**: 服务不可用时自动回退到客户端 `storyboard-prompt.ts`
- **统一接口**: `storyboardCompose(scenes, fullText, strategy)` → `StoryboardResult[]`
- **策略枚举**: `storyboardStrategies()` 获取可用策略列表

### 7.19.3 API 接口

| 函数 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `threeStepMetaphor(concept)` | string | MetaphorResult | 概念→分镜三步转换 |
| `composeStoryboardPrompt(concept, options?)` | string, StoryboardOptions | StoryboardPrompt | 完整分镜 prompt |
| `generateCandidates(concept, count=3)` | string, number | StoryboardPrompt[] | 批量候选+去重 |
| `renderPromptString(prompt)` | StoryboardPrompt | string | 渲染为 LLM 输入 |

### 7.19.4 集成状态

**当前状态：已集成到 CreatePage 生成管线**

CreatePage Step 4（图片提示词生成）新增 optional storyboard 路径：
- 默认: `generateImagePrompts()` (v9.0 客户端策略，维持原行为)
- 开启 storyboard: `storyboardCompose()` 替代，通过 localStorage 控制

**启用方式（localStorage）：**
| 键 | 值 | 效果 |
|----|----|------|
| `storyboard_enabled=1` | `"1"` | 开启 storyboard compose（默认关闭） |
| `storyboard_strategy` | `"xiaohei_storyboard"` | 选择策略，默认小黑插画风 |
| `storyboard_service=1` | `"1"` | 使用 prompt-engine API（默认客户端降级） |

**架构：**
```
CreatePage → storyboardCompose(scenes, fullText, strategy)
  ├─ [API 模式] fetch → prompt-engine /v1/storyboard/compose
  │     └─ 失败降级 → storyboard-prompt.ts（客户端）
  └─ [客户端模式] → storyboard-prompt.ts（客户端渲染）
```

**后续规划：**
- 策略选择 UI（CreatePage 设置面板中的下拉选择）
- 策略预览（显示当前构图的视觉描述）

### 7.19.5 Feature Gate 迁移说明（新增）

**问题：** 当前 storyboard 功能通过 `localStorage` 控制开关，仅适用于开发调试，不适用于生产环境。

**迁移方案：** 将功能开关从 localStorage 迁移到 orchestrator 的 `feature_gates.yaml`。

| 旧方案（localStorage） | 新方案（feature_gates.yaml） | 说明 |
|----------------------|---------------------------|------|
| `storyboard_enabled=1` | `storyboard` gate | 通过会员等级控制，不再由用户自行开启 |
| `storyboard_strategy` | `storyboard_strategy` gate 参数 | 后端下发可用策略列表 |
| `storyboard_service=1` | 自动降级 | API 不可用时自动回退客户端，无需开关 |

**迁移步骤：**
1. 在 `feature_gates.yaml` 中新增 `storyboard` gate（tier: 基础版）
2. Story2Video 前端 `useFeatureGate('storyboard')` 替代 localStorage 读取
3. 保留 localStorage 作为开发调试后门（仅 `NODE_ENV=development` 时生效）
4. 完全移除生产环境对 localStorage 功能开关的依赖

**同样需迁移的 localStorage 开关：**
- `orchestrator_url` / `orchestrator_api_key` → 环境变量 `VITE_ORCHESTRATOR_URL`（生产环境）
- `watermark_*` 系列 → 后端用户配置表（跨设备同步）

---

## 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v1.0.0 | 2026-06-01 | 初始版本 |
| v1.1.0 | 2026-06-15 | 新增分段视频模式、音频生成模式、发布功能、会员系统、Ian 小黑分镜 |
| v1.2.0 | 2026-07-02 | 审查报告修复：补充视频时长限制(4.8.1)、音频文件限制(4.3.1)、并发策略(4.11)、会员配额定义(7.18.2a)、数据模型扩展(4.8a)、发布状态机(7.20)、Feature Gate 迁移(7.19.5)；新增异常边界用例 |

> **注：** 本文档仅描述产品需求，不包含实现状态。实现进度请查阅 CHANGELOG.md 和各模块 AGENTS.md。
### 7.19.4 集成路线
- 当前模块独立可用，尚未集成到前端 CreatePage
- 后续集成点：CreatePage 文案输入阶段的"开启小黑分镜"开关
